//! mtr-core — everything not yet split into its own crate. Deliberately a
//! single blob for now; pieces (mutators, instrumentation, coverage, cache,
//! config, runners, reporters, engine) get their own crate only once a
//! build stage gives them enough real content to justify the split.
